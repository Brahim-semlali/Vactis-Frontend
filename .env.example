# En développement : lancer le backend sur le port 8082 ; Parcel proxifie /api (fichier .proxyrc).
# Origines autorisées côté backend (variable CORS_ALLOWED_ORIGINS) :
#   http://localhost:5173,http://127.0.0.1:5173
